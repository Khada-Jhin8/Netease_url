services:
  netease-url:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - /opt/Navidrome/music:/opt/Navidrome/music
    restart: unless-stopped
